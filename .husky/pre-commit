#!/usr/bin/env sh
npx lint-staged

# Spec traceability runs against the whole spec tree (not just staged files),
# so it catches drift anywhere in openspec/. Pure filesystem check — no network.
npm run lint:specs
